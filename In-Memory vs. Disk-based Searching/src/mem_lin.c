#include <stdio.h>
#include <stdlib.h>
#include "mem_lin.h"
#include "load.h"

void search(const char* key, const char* seek) {
    
    
    // // free malloc address
    // free(a.array);
    // a.array = NULL;
    // a.size = 0;
}

